# Opt-in workspace config; merge into the selected workspace's .codex/config.toml.
# Role goals: references/luna-xhigh.md. Leaves: references/worker-agents/.
model = "gpt-6-luna"
model_reasoning_effort = "xhigh"

[agents]
default_subagent_model = "gpt-6-luna"
default_subagent_reasoning_effort = "xhigh"
