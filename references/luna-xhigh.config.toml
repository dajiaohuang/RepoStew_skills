# Opt-in CLI profile; install as CODEX_HOME/repostew-luna-xhigh.config.toml.
# Role goals: references/luna-xhigh.md. Leaves: references/worker-agents/.
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"

[agents]
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "xhigh"
