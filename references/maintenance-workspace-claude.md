# CLAUDE.md entry

Render the [workspace template](maintenance-workspace-agents.md), then use:

```markdown
Read workspace AGENTS.md and its canonical RepoStew skill/required references.
Complete current inline leaf content satisfies reading; no duplicate workflow,
user-home installation or transcript-derived state.
As a CLI leaf, execute only the packet; no children/shared-state writes.
Native Luna configuration does not select this CLI's provider/model.
```
